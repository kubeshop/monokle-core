{
  "name": "monokle-core",
  "private": false,
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "release": "turbo run build && changeset publish"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "prettier": "2.5.1",
    "pretty-quick": "3.1.3",
    "turbo": "1.10.13"
  },
  "dependencies": {
    "@changesets/cli": "2.25.2"
  }
}
