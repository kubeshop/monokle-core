{
  "name": "monokle-core",
  "private": false,
  "scripts": {
    "build": "turbo run build",
    "release": "turbo run build && changeset publish"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "prettier": "2.5.1",
    "pretty-quick": "3.1.3",
    "turbo": "1.6.3"
  },
  "dependencies": {
    "@changesets/cli": "2.25.2"
  }
}
